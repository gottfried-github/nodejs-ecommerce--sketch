# Types
## `id`
a valid BSON `objectId`

## `fields`
An object with the following properties:
* `isInSale`: `boolean`
* `name`: `string`
* `itemInitial`: a valid BSON `objectId`

# update
## parameters
    1. **id:** `id`
    2. **fields:** `fields`
